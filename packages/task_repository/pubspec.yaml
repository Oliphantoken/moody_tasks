name: task_repository
description: (Backend) The repository that handles tasks repository
publish_to: 'none'
version: 0.0.1


environment:
  sdk: '>=3.1.0 <4.0.0'


dependencies:
  flutter:
    sdk: flutter

  # SharedPreferences is a local storage plugin
  shared_preferences: ^2.2.2

  # SQFLite is a lite SQL database
  sqflite: ^2.3.0
  path: ^1.9.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  